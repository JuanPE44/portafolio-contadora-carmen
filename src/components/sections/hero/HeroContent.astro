---
interface Props {
  nombre: string;
  titulo: string;
  descripcion: string;
}

const { nombre, titulo, descripcion } = Astro.props;
---
<h1 class="text-4xl mt-10 md:mt-0 lg:text-4xl font-bold text-gray-900 text-left mb-2">
  Impulsá tu <span class="text-primary">negocio</span> con un <span class="text-primary">asesoramiento impositivo</span> sólido
</h1>
<p class="text-base smtext-lg text-gray-800 mb-8 text-left leading-relaxed">
  {descripcion}
</p>